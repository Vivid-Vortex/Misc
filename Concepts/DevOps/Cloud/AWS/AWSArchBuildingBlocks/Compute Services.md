ECS (Elastic Continer Service) - It's AWSs own product for K8s replacement.
ECR (Elastic Continer Repository) - It's AWS own product replacement of JFrog artifactory.
EKS (Elastic K8s Service) - It's actually using Open source K8s (since it's open source) and managing it's so it's managed K8s on AWS.
