1. To keep a code simple, sweet and easy to understand for a java developer perspective.

```
**TypeScript Coding Guideline:**
Keep TypeScript simple, readable, maintainable, and Java-like. Prefer straightforward types, interfaces, basic generics, arrays, objects, Map, Set, and common built-in utility types such as Partial, Record, Pick, and Omit when they simplify the code. Avoid advanced TypeScript type-level programming, complex mapped/conditional types, clever type tricks, excessive generics, and unnecessary abstractions. Prioritize code that is easy to understand and maintain over highly clever or sophisticated TypeScript. Only use advanced TypeScript features when they are genuinely required by the problem, framework, or library. You are free to use any built in Types though like Record, Set, Map, any uutlity tupes like Partial etc. If you are using any built in type apart from Record, Set, Map, Array, Array, Function, Date then please put a comment on top of the declaration of it saing built in type and tive not more than 1 lie of simple explanation about this and also mention something like htese are utility types as Partial come under untitly types and like that for any othre type. Not need to give comment for Java like built in types or Record, Set, Map, Array, Array, Function, Date. Always prefer built in types over trying to come up with your own complex type. Also intead of using long or complex notations of any types like Array<string> you shoudl use string[] and do the same for other such types.
```

---
